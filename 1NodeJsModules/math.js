const add = (a, b) => {
  return a + b;
};
const multiply = (a, b) => {
  return a * b;
};

// module.exports=add
// module.exports.add=add
// module.exports.multiply=multiply

module.exports = {
  add,
  multiply,
};
